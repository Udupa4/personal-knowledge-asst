# Server
API_KEY=<your_api_key>

# Redis
REDIS_URL=redis://localhost:6379/0
STM_TTL_SECONDS=86400

# LLM provider: "google" or "ollama"
LLM_PROVIDER=ollama
LLM_MAX_TOKENS=512

# Google Gemini (used when LLM_PROVIDER=google)
GOOGLE_API_KEY=<your_google_api_key>
GEMINI_MODEL=gemini-2.5-flash-lite

# Ollama (used when LLM_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen2.5-coder:14b

# Embeddings provider: "google" or "huggingface"
EMBEDDING_PROVIDER=huggingface
GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001

# Storage paths
CHROMA_PERSIST_DIR=./chroma_store
LTM_PERSIST_DIR=./ltm_store

# LangSmith observability
# Get your key at https://smith.langchain.com
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=<your_langsmith_api_key>
LANGCHAIN_PROJECT=personal-knowledge-asst